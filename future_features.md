# Future Features

- add support for additional languages (JavaScript, Python, Go, Rust) with language-aware context extraction.
- get `ours/theirs/base` git information and pass it to the model 
- Add optional Git metadata per conflict (branch names, commit SHAs, author hints).
- Add ignore/include configuration file support (for custom directories and file globs).
